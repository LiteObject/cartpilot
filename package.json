{
  "name": "cartpilot",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension for building grocery carts with deterministic automation and local LLM support.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.325",
    "dotenv": "^8.2.0",
    "esbuild": "^0.28.1",
    "node-fetch": "^3.3.2",
    "typescript": "^5.8.3"
  }
}
